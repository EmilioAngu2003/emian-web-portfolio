---
interface Props {
  id: string;
  sprite?: string;
  class?: string;
}

const { id, sprite, class: className } = Astro.props;
---

<svg class={className} aria-hidden="true">
  <use xlink:href={sprite ? `/sprites/${sprite}.svg#${id}` : `#${id}`}></use>
</svg>
